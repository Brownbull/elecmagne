# Electric potential energy
diff V = diff U / q0 = -W(a,b) / q0 # Work of electrostatic force from Q to q0 when q0 moves from a to b
# se mide en Volt = Joule / Coulumb
# Voltaje = Potencial

# Potencial en Cargas Puntuales
V[b] - V[a] = (U[b] - U[a]) / q0 = KQ (1/r[a] - 1/r[b])

# para una carga 
V[i] = Q[i] / r[i]

#  para varias
V[total] = K * (for i..n, sum = 0 ( sum = sum + Q[i] / r[i] ))
